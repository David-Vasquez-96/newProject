import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme=>({
    containerPrincipal:{
        textAlign: 'center',
        background: 'white',
        padding: '20px',
    },
    containerTable: {
        margin: '20px 0px',
    },     
}));